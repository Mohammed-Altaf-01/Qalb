import SettingsClient from "./SettingsClient";

export const metadata = {
  title: "Settings · Qalb",
  description: "Theme, text size, and app preferences.",
};

export default function SettingsPage() {
  return <SettingsClient />;
}
